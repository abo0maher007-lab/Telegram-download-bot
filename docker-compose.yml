version: '3.8'

services:
  bot:
    build: .
    container_name: telegram-downloader
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - ./downloads:/app/downloads
    restart: unless-stopped
